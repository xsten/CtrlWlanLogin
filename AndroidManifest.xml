<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.stenuit.xavier.cetrelwlanlogin"
    android:versionCode="100"
    android:versionName="1.00 work" >
    <!-- V0.03 - 20130423 - Delivered by email (src and apk) to Florian -->
	<!-- V0.04 - 20130424 - Gets the filename when starting the app (not when displaying settings screen) -->
	<!-- V1.00 - 20130711 - First version uploaded uploaded in github -->
    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="net.stenuit.xavier.cetrelwlanlogin.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="net.stenuit.xavier.cetrelwlanlogin.SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>
    </application>

</manifest>